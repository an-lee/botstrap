# Agent instructions

This repository uses Botstrap defaults. Keep changes focused, match existing style, and prefer non-interactive commands in automation.

- Read `README.md` and `docs/ARCHITECTURE.md` before large changes.
- Use the project’s package manager and formatter conventions.
- Do not commit secrets; use environment variables or sops/age where applicable.
